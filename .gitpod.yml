tasks:
  - init: echo "Replace me with a build script for the project."
    init: cd /workspace
    init: git clone https://github.com/dragnet-org/dragnet.git
    init: pip install -r dragnet/requirements.txt
    init: cd dragnet
    init: make install
    init: pip install scrapy pymongo 
    command: echo "Replace me with something that should run on every start, or just
      remove me entirely."
  - command: mkdir -p /workspace/data && mongod --dbpath /workspace/data

image:
  file: .gitpod.Dockerfile
